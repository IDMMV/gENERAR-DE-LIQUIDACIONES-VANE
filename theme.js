(()=>{const key='liquidaciones-vane-mode';const btn=document.getElementById('modeBtn');const setMode=mode=>{document.body.setAttribute('data-mode',mode);localStorage.setItem(key,mode);if(btn)btn.textContent=mode==='dark'?'☾ Modo noche':'☀ Modo día'};let saved='dark';try{saved=localStorage.getItem(key)||'dark'}catch(e){}setMode(saved);btn?.addEventListener('click',()=>setMode(document.body.getAttribute('data-mode')==='dark'?'light':'dark'))})()