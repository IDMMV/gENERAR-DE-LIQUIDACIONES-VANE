(()=>{const $=x=>document.getElementById(x),S={wb:null,buf:null,name:'',rows:[],f:{ins:'',per:'',area:'',sed:''}};const N=x=>String(x??'').trim(),K=x=>N(x).toUpperCase(),U=a=>[...new Set(a.map(N).filter(Boolean))],num=x=>{if(typeof x==='number')return x;let n=Number(N(x).replace(/S\/?\.?/gi,'').replace(/,/g,''));return isNaN(n)?0:n},money=x=>'S/ '+Number(x||0).toLocaleString('es-PE',{minimumFractionDigits:2,maximumFractionDigits:2});
function sh(n){let k=S.wb.SheetNames.find(x=>K(x)==K(n));return k?S.wb.Sheets[k]:null}function rr(ws){let a=XLSX.utils.sheet_to_json(ws,{header:1,defval:'',raw:true});if(!a.length)return[];let h=a[0].map(K);return a.slice(1).filter(r=>r.some(x=>N(x))).map((r,i)=>{let o={row:i+2};h.forEach((x,j)=>o[x]=r[j]??'');return o})}function F(r,a){for(let x of a)if(K(x)in r)return r[K(x)];return''}
function loadRows(){return rr(sh('DETALLE')).map((r,i)=>({ins:N(F(r,['INSPECTOR'])),per:N(F(r,['PERIODO','PERIODO / MES'])),area:N(F(r,['TELECONTROL / PROTECCION','TELECONTROL / PROTECCIÓN','AREA','ÁREA']))||'SIN ÁREA',sed:N(F(r,['SED']))||'SIN SED',wp:N(F(r,['WP-BAREMOS','WP-BAREMO','BAREMO']))||'SIN BAREMO',date:F(r,['FECHA']),ord:N(F(r,['NUMERO DE ORDEN','N° DE ORDEN','ORDEN']))||`ROW-${i+1}`,amt:num(F(r,['VALOR PROYEC (S/.)','VALOR PROYEC','VALOR PROYECTADO','VALOR'])),obs:N(F(r,['OBSERVACIONES','ESTADO']))})).filter(r=>r.ins||r.per||r.sed)}
function A(a,f){let m={};a.forEach(r=>{m[r[f]]=(m[r[f]]||0)+r.amt});return Object.entries(m).map(([label,value])=>({label,value})).sort((a,b)=>b.value-a.value)}function filtered(){return S.rows.filter(r=>Object.entries(S.f).every(([k,v])=>!v||K(r[k])==K(v)))}
function bars(el,data,cls){let d=data.slice(0,10),mx=Math.max(1,...d.map(x=>x.value));$(el).innerHTML=d.map(x=>`<div class=bar><span>${N(x.label).slice(0,18)}</span><div class=track><div class="fill ${cls||''}" style="width:${x.value/mx*100}%"></div></div><b>${money(x.value)}</b></div>`).join('')||'<i>Sin datos</i>'}
function render(){let a=filtered(),tot=a.reduce((s,r)=>s+r.amt,0),days=U(a.map(r=>{let d=new Date(r.date);return isNaN(d)?N(r.date):d.toDateString()})).length,ords=U(a.map(r=>r.ord)).length,seds=U(a.map(r=>r.sed)).length;$('kpis').innerHTML=[['Producción total',money(tot)],['Órdenes únicas',ords],['SED atendidas',seds],['S/ por día trabajado',money(days?tot/days:0)],['Órdenes / inspector / día',(days?ords/days:0).toFixed(2)]].map(x=>`<div><small>${x[0]}</small><b>${x[1]}</b></div>`).join('');bars('inspectors',A(a,'ins'),'purple');bars('areas',A(a,'area'),'green');let p=A(a,'wp').slice(0,12),sum=p.reduce((s,x)=>s+x.value,0)||1,c=0;$('pareto').innerHTML=p.map(x=>(c+=x.value,`<div class=bar><span>${x.label}</span><div class=track><div class="fill amber" style="width:${x.value/sum*100}%"></div></div><b>${(c/sum*100).toFixed(0)}%</b></div>`)).join('')||'<i>Sin datos</i>';let sm={};a.forEach(r=>sm[r.sed]=(sm[r.sed]||0)+r.amt);let v=Object.values(sm),mx=Math.max(...v,0),step=mx/6||1,b=Array(6).fill(0);v.forEach(x=>b[Math.min(5,Math.floor(x/step))]++);let bm=Math.max(...b,1);$('hist').innerHTML=b.map((n,i)=>`<div class=bar><span>${money(i*step)}</span><div class=track><div class="fill amber" style="width:${n/bm*100}%"></div></div><b>${n}</b></div>`).join('');let h=Array.from({length:5},()=>Array(7).fill(0));a.forEach(r=>{let d=new Date(r.date);if(isNaN(d))return;let w=(d.getDay()+6)%7,q=Math.min(4,Math.floor((d.getDate()-1)/7));h[q][w]+=r.amt});let hm=Math.max(1,...h.flat());$('heat').innerHTML=`<div class=heat><div></div>${['Lun','Mar','Mié','Jue','Vie','Sáb','Dom'].map(x=>`<div class=lab>${x}</div>`).join('')}${h.map((r,i)=>`<div class=lab>Semana ${i+1}</div>`+r.map(x=>`<div style="background:rgba(42,120,214,${x?.12+.88*x/hm:.03})">${x?Math.round(x):''}</div>`).join('')).join('')}</div>`;$('eff').innerHTML=`<div class=bar><span>S/ por día</span><div class=track><div class="fill green" style="width:${Math.min(100,days?tot/days/(Math.max(tot/days,1))*100:0)}%"></div></div><b>${money(days?tot/days:0)}</b></div><p class=note>Productividad relativa calculada sobre los registros filtrados.</p>`;let prev=U(S.rows.map(r=>r.per)).filter(x=>K(x)!=K(S.f.per)).pop(),cur=A(a,'area'),pa=prev?A(S.rows.filter(r=>K(r.per)==K(prev)&&(!S.f.ins||K(r.ins)==K(S.f.ins))),'area'):[];let cm=Object.fromEntries(cur.map(x=>[x.label,x.value])),pm=Object.fromEntries(pa.map(x=>[x.label,x.value]));let keys=U([...cur,...pa].map(x=>x.label)).slice(0,10),mxx=Math.max(1,...keys.flatMap(k=>[cm[k]||0,pm[k]||0]));$('compare').innerHTML=keys.map(k=>`<div class=bar><span>${k}</span><div class=track><div class=fill style="width:${(cm[k]||0)/mxx*100}%"></div><div class="fill purple" style="width:${(pm[k]||0)/mxx*100}%;margin-top:3px"></div></div><b>${money(cm[k]||0)}</b></div>`).join('')+`<small>Azul actual · morado anterior ${prev||'no disponible'}</small>`;let q=K($('search').value),z=a.filter(r=>!q||K(r.sed+' '+r.ins+' '+r.ord).includes(q));$('tbody').innerHTML=z.slice(0,150).map(r=>`<tr><td>${r.sed}</td><td>${r.ins}</td><td>${money(r.amt)}</td><td class="${/venc/i.test(r.obs)?'bad':r.obs?'warn':'ok'}">${/venc/i.test(r.obs)?'VENCIDO':r.obs?'OBSERVADO':'COMPLETADO'}</td><td>${r.ord}</td></tr>`).join('')||'<tr><td colspan=5>Sin registros</td></tr>'}
function setup(){let defs=[['ins','Inspector'],['per','Periodo'],['area','Área'],['sed','SED']],box=$('filters');box.innerHTML='<div>'+defs.map(([k,l])=>`<label>${l}<select data-k=${k}><option value="">Todos</option>${U(S.rows.map(r=>r[k])).map(x=>`<option>${x}</option>`).join('')}</select></label>`).join('')+'</div>';box.querySelectorAll('select').forEach(x=>x.onchange=()=>{S.f[x.dataset.k]=x.value;render()});['ins','per'].forEach(k=>{$(k).innerHTML='<option value="">Selecciona</option>'+U(S.rows.map(r=>r[k])).map(x=>`<option>${x}</option>`).join('');$(k).disabled=false;$(k).onchange=e=>{S.f[k]=e.target.value;render();updatePreview()}})}
function updatePreview(){let a=S.f.ins&&S.f.per?S.rows.filter(r=>K(r.ins)==K(S.f.ins)&&K(r.per)==K(S.f.per)):[];$('prevbox').textContent=a.length?`${a.length} registros DETALLE listos para ${S.f.ins} · ${S.f.per}. El libro original será preservado.`:'Selecciona Inspector + Periodo.';$('badge').textContent=a.length?'Liquidación preparada':'Esperando selección'}
function headerInfo(ws){let a=XLSX.utils.sheet_to_json(ws,{header:1,defval:'',raw:true});if(!a.length)return null;return{head:a[0].map(K),rows:a.slice(1).map((r,i)=>({row:i+2,values:r}))}}
function valueByHead(values,heads,names){for(const n of names){const i=heads.indexOf(K(n));if(i>=0)return N(values[i])}return''}
function rowKeepForSheet(name,info,allowedSeds){if(!info)return new Set();const keep=new Set();for(const r of info.rows){const ins=valueByHead(r.values,info.head,['INSPECTOR']),per=valueByHead(r.values,info.head,['PERIODO','PERIODO / MES']),sed=valueByHead(r.values,info.head,['SED']);let keepRow=true;if(ins||per){if(S.f.ins&&ins&&K(ins)!==K(S.f.ins))keepRow=false;if(S.f.per&&per&&K(per)!==K(S.f.per))keepRow=false}else if(/^(MAT|RECUPERO)$/i.test(name)&&sed){keepRow=allowedSeds.has(K(sed))}if(keepRow)keep.add(r.row)}return keep}
function normalizeTarget(target){target=target.replace(/^\//,'');if(target.startsWith('xl/'))return target;if(target.startsWith('../'))return 'xl/'+target.replace(/^\.\.\//,'');return 'xl/'+target}
async function sheetPaths(zip){const wx=zip.file('xl/workbook.xml'),rx=zip.file('xl/_rels/workbook.xml.rels');if(!wx||!rx)throw Error('El libro no tiene una estructura Office Open XML válida.');const wd=new DOMParser().parseFromString(await wx.async('string'),'application/xml'),rd=new DOMParser().parseFromString(await rx.async('string'),'application/xml'),rels={};rd.getElementsByTagName('Relationship');for(const el of Array.from(rd.getElementsByTagName('Relationship')))rels[el.getAttribute('Id')]=el.getAttribute('Target');const out={};for(const el of Array.from(wd.getElementsByTagName('sheet'))){const name=el.getAttribute('name'),rid=el.getAttribute('r:id')||el.getAttributeNS('http://schemas.openxmlformats.org/officeDocument/2006/relationships','id');if(name&&rid&&rels[rid])out[name]=normalizeTarget(rels[rid])}return out}
function hideRowsXml(xml,keep,dataRows){const rows=new Set(dataRows);return xml.replace(/<row\b[^>]*>/g,m=>{const rm=m.match(/\br="(\d+)"/);if(!rm)return m;const r=Number(rm[1]);if(!rows.has(r))return m;const hidden=/\bhidden="[^"]*"/.test(m);return hidden?m.replace(/\bhidden="[^"]*"/,'hidden="1"'):m.replace(/>$/,' hidden="1">')})}
async function exportExact(){if(!S.buf){alert('Carga primero el archivo Excel.');return}if(!S.f.ins||!S.f.per){alert('Selecciona Inspector y Periodo antes de generar la liquidación.');return}if(!window.JSZip){alert('No se cargó el motor de preservación del Excel. Actualiza la página e inténtalo nuevamente.');return}const isOOXML=/\.(xlsx|xlsm)$/i.test(S.name);if(!isOOXML){alert('Para conservar exactamente formato, colores, imágenes, fórmulas y macros, esta versión requiere .xlsx o .xlsm.');return}const targetNames=['DETALLE','MAT','RECUPERO'];const allowedSeds=new Set(S.rows.filter(r=>K(r.ins)==K(S.f.ins)&&K(r.per)==K(S.f.per)).map(r=>K(r.sed)));const zip=await JSZip.loadAsync(S.buf);const paths=await sheetPaths(zip);let changed=0;for(const logical of targetNames){const real=S.wb.SheetNames.find(x=>K(x)==K(logical));if(!real||!paths[real])continue;const info=headerInfo(S.wb.Sheets[real]);if(!info)continue;const dataRows=new Set(info.rows.map(r=>r.row));const keep=rowKeepForSheet(real,info,allowedSeds);const file=zip.file(paths[real]);if(!file)continue;const xml=await file.async('string');zip.file(paths[real],hideRowsXml(xml,new Set([...dataRows].filter(r=>!keep.has(r))),dataRows));changed++}const out=await zip.generateAsync({type:'blob',compression:'DEFLATE',compressionOptions:{level:6}});const ext=/\.xlsm$/i.test(S.name)?'.xlsm':'.xlsx';const safe=(S.f.ins||'INSPECTOR').replace(/[^\wáéíóúÁÉÍÓÚñÑ -]/g,'').trim().replace(/\s+/g,'_');const per=(S.f.per||'PERIODO').replace(/[^\wáéíóúÁÉÍÓÚñÑ -]/g,'').trim().replace(/\s+/g,'_');const filename=S.name.replace(/\.[^.]+$/,'')+' - '+safe+' - '+per+ext;const a=document.createElement('a');a.href=URL.createObjectURL(out);a.download=filename;document.body.appendChild(a);a.click();setTimeout(()=>{URL.revokeObjectURL(a.href);a.remove()},1500);$('badge').textContent='Archivo generado';$('prevbox').textContent=`Liquidación ${S.f.ins} · ${S.f.per} generada. Se preservó el libro original y se filtraron ${changed} hojas de trabajo.`}
$('up').onclick=()=>$('file').click();$('file').onchange=async e=>{let f=e.target.files[0];if(!f)return;try{S.buf=await f.arrayBuffer();S.name=f.name;S.wb=XLSX.read(S.buf,{type:'array',cellDates:true,bookVBA:/\.xlsm$/i.test(f.name)});S.rows=loadRows();S.f={ins:'',per:'',area:'',sed:''};$('name').textContent=f.name;$('meta').textContent=S.rows.length+' registros DETALLE · '+S.wb.SheetNames.length+' hojas · archivo local';$('gen').disabled=$('exp').disabled=false;$('preview').disabled=false;$('badge').textContent='Archivo listo';setup();render();updatePreview()}catch(err){console.error(err);alert('No se pudo leer el archivo: '+err.message)}};$('clear').onclick=()=>{S.f={ins:'',per:'',area:'',sed:''};$('filters').querySelectorAll('select').forEach(x=>x.value='');$('ins').value='';$('per').value='';render();updatePreview()};$('search').oninput=render;$('gen').onclick=exportExact;$('exp').onclick=exportExact;$('preview').onclick=updatePreview;})();